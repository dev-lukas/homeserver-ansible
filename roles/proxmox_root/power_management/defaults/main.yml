---
# Let idle cores reach deep C-states: halt_poll_ns=0 stops KVM busy-polling
# before halting an idle vCPU, nmi_watchdog=0 drops the per-core lockup NMIs.
power_kvm_cstate_tuning_enabled: true
power_kvm_halt_poll_ns: 0
power_nmi_watchdog: 0
