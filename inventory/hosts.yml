---
all:
  children:
    proxmox:
      hosts:
        proxmox-root:
          ansible_host: 192.168.178.56
          ansible_user: root
          # Fresh install (no deploy key yet): add --ask-pass for the root password.
          ansible_ssh_common_args: "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null"
