---
internal_subnet: "192.168.178.0/24"

powertop_auto_tune: true
# Kernel-native NIC ASPM; needs the hidden BIOS "Native ASPM" = Enabled
# (see roles/proxmox_root/power_management/tasks/nic_pm.yml).
nic_pm_enabled: true
ltr_ignore_enabled: true
